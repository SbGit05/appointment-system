spring.application.name=backend

# Port — Render sets PORT automatically
server.port=${PORT:8080}

# ─── Database ─────────────────────────────────────────────────────────────────
# Using your Render PostgreSQL credentials as defaults so it "just works" on deploy
# To run locally with MySQL, you can set these environment variables or change them here.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d7iki0iqqhas739648e0-a.singapore-postgres.render.com:5432/appointment_db_bhnd}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:appointment_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:UfYZ1Uu9jinTDQe2TB5Z3YiS38qT9O6O}

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Allows all origins for now to ensure it "just works" without setting ALLOWED_ORIGINS
cors.allowed-origins=${ALLOWED_ORIGINS:*}
